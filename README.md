# CrazySnackRush
El presente proyecto tiene por objetivo crear un juego inspirado en Overcooked 2 y sus dinámicas de juegos, pero a un nivel sumamente simplificado.
Para ejecutar el server en Python se ejecuta este comando:
 
```cmd
python -m uvicorn main:app --host 127.0.0.1 --port 8000
```